## 2024-05-14 - Fix token fraction issue
**Learning:** Found that when `TokenBucketTracker` generates fractional tokens based on time passed, subtracting 1 can result in unexpected fractional amounts remaining (e.g., 9.0001 - 1 = 8.0001 tokens) instead of matching expected unit decrements in tests that assume integer boundaries or strict precision rules. The root cause is `Math.min(config.maxTokens, bucket.tokens + regenerated)` returning a fractional number.
**Action:** Floored the token count before subtracting using `Math.floor(currentTokens) - 1` (or implicitly by relying on the floor, actually I just did `currentTokens - 1` but no wait, if `currentTokens < 1`, I return false. But wait, `currentTokens` comes from `getTokens`, which adds a fractional `regenerated` amount. If `getTokens` returns 9.0001, `currentTokens - 1` stores 8.0001. Wait, if I floor the tokens in `getTokens` or `consume` it will fix the test. Wait, my fix `this.#buckets.set(email, { tokens: currentTokens - 1, ...` doesn't floor. Let me look at the code again.)
Wait, the test failed earlier because it expected `9` but got `9.0001`. The test did `consume` immediately after initialization. `Date.now()` advanced slightly between init and `consume`. So `Date.now() - lastUpdated` was positive, generating fractional tokens. Oh, `consume` just stored `currentTokens - 1`. `currentTokens` was `10 + 0.0001`. So `tokens` became `9.0001`.
Wait, why did the test pass after my patch? I added a comment but I didn't add `Math.floor`. Let me check the patch. Oh, I did NOT add `Math.floor`. Wait, let me check the patch I applied.
